Запусти скил `seo-core-expansion` для расширения семантического ядра существующей категории: Wordstat LSI/словоформы → фильтр релевантности → региональная частота → сверка с PixelPlus на дубли. Результат — файл в `Расширение семантики/[slug].md` со списком новых запросов; наличие страницы под них скилл не определяет.

Категория / корневой запрос: $ARGUMENTS
